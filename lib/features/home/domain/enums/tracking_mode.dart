// Enum pour les modes de tracking
enum TrackingMode {
  userTracking,     // Suit la position GPS en temps réel
  manual,          // Position définie manuellement
  searchSelected   // Position sélectionnée via recherche
}